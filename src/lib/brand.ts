/**
 * Brand tokens shared across TS/HTML.
 *
 * NOTE: The brand identity in this project is a dark, near-black wordmark and
 * lightning-bolt mark on white (Space Mono type). There is no burgundy in the
 * supplied assets, so the full-screen loader uses the real dominant brand tone
 * to stay on-brand. To switch to a different loader colour, change LOADER_BG in
 * one place here and mirror it in index.html (#initial-loader background).
 */
export const LOADER_BG = '#15181a'

/** Simplified brand mark (lightning bolt). Rendered white via CSS filter. */
export const LOADER_LOGO = '/img/favicon1.png'
