# Git LFS removido - los archivos ahora se commitean normalmente
# Si necesitas Git LFS en el futuro, descomenta las líneas siguientes:
# public/uploads/photography/**/*.{jpg,JPG,png,PNG,jpeg,JPEG} filter=lfs diff=lfs merge=lfs -text
# *.webp filter=lfs diff=lfs merge=lfs -text
