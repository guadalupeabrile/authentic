{
    "buildCommand": "npm run build",
    "rewrites": [
        {
            "source": "/((?!api/).*)",
            "destination": "/index.html"
        }
    ],
    "functions": {
        "api/[...path].ts": {
            "maxDuration": 30
        }
    }
}